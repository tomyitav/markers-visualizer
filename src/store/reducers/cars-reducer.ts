import {Car} from "../../model/Car";

const carsReducer = (state: Car[] = [], action: any) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default carsReducer;